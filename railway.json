{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "phases": {
        "install": {
          "cmds": ["pip install -r requirements.txt"]
        },
        "start": {
          "cmds": ["python3 bot.py"]
        }
      }
    }
  }
}
